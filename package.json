{
  "name": "tictactoe",
  "version": "1.0.0",
  "description": "TicTacToe game with real-time multiplayer",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && node server.js",
    "postinstall": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
